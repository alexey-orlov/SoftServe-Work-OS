# Feature Index
#
# Master lookup for every feature your team is building. Maps each feature to
# all related artifacts so any teammate (or agent) can find what exists in one
# read instead of grepping the repo.
#
# Structure: product-area → sub-feature → all related artifacts
#
# Conventions
# - Keep this file authoritative. Whenever a feature ships, the launch gate
#   verifies its entry here.
# - This file is gated in the write policy: agents confirm changes with the
#   user before writing — see governance/write-policy.yaml.
# - Paths are relative to product-development/.
# - tickets references your tracker (Linear / Jira / Asana). Pick one format.
# - Recognized artifact keys: prd, eng-plan, figma, tickets, table-schemas,
#   queries, metrics, dashboards, experiments, investigations,
#   decisions, initiatives — plus:
#     reviews:          review artifacts in product/PRDs/{area}/reviews/ (assumption map,
#                       red-team, challenge reports, pre-mortem, brief reviews), listed as they land
#     impact-sizing:    analytics/investigations/{area}/impact-sizing-*.md
#     launch-checklist: product/launches/{slug}-launch-checklist.md
#     launch-gate:      product/launches/{slug}-gate-{date}.md verdict
#     breakdown:        product/PRDs/{area}/{initiative-slug}-breakdown.md (/feature-breakdown)
#     briefs:           list of product/PRDs/{area}/{initiative-slug}-{feature-slug}-brief.md (/feature-brief)
# - Omit keys that don't apply yet. Add them when artifacts exist.

# Replace these examples with your own product areas. Keep one or two as a
# template for new entries.

billing:
  credit-usage-dashboard:
    # The starter template ships every artifact for this feature so the
    # /feature-launch-gate gate passes on the example out of the box.
    prd: product/PRDs/billing/credit-usage-dashboard-prd.md
    # Figma references. Use file IDs (the abc123 in the URL), not display
    # names — file IDs survive renames. For specific frames, add a node-id:
    # https://figma.com/file/abc123/?node-id=42:128
    # Multiple Figma artifacts per feature is fine — separate by purpose.
    figma:
      flow: https://figma.com/file/your-figma-file
      prototype: https://figma.com/proto/your-figma-file
      components: https://figma.com/file/your-design-system-file/?node-id=42:128
    tickets:
      - YOURPROJ/123
      - YOURPROJ/124
    table-schemas:
      - analytics/schemas/billing/billing_events.md
    queries:
      - analytics/queries/billing/churn_by_segment.sql
    metrics:
      - analytics/metrics/billing/billing-metrics.md
    dashboards: analytics/dashboards/billing/credit-usage-dashboards.md
    experiments:
      - analytics/experiments/billing/low-balance-warning-2026-03-05-experiment-design.md
      - analytics/experiments/billing/low-balance-warning-2026-03-05-experiment-results.md
    investigations:
      - analytics/investigations/billing/2026-03-10-credit-depletion-churn-analysis.md
    decisions:
      - product/decisions/2026-02-14-usage-based-pricing.md
    initiatives: [credit-usage-dashboard-v1]

  tier-discount-promo:
    # Original 2026-05-13 launch target slipped — /feature-launch-gate is
    # BLOCKED until the PRD + eng plan land. Analyst-side artifacts present;
    # PRD + eng plan owned by PM/Eng leads.
    table-schemas:
      - analytics/schemas/billing/promo_offers.md
    queries:
      - analytics/queries/billing/promo_conversion_rate.sql
    metrics:
      - analytics/metrics/billing/tier-discount-promo-metrics.md
    initiatives: [tier-discount-promo]

workforce:
  time-off-requests:
    # EXAMPLE (synthetic, fictional product Beacon) — the worked example of the
    # definition→delivery bridge: PRD → /feature-breakdown → /feature-brief.
    prd: product/PRDs/workforce/time-off-requests-prd.md
    breakdown: product/PRDs/workforce/time-off-requests-breakdown.md
    briefs:
      - product/PRDs/workforce/time-off-requests-request-approval-brief.md   # F-1 request-approval
    initiatives: [time-off-requests]

# Add more product areas here. Common starting set:
# onboarding:
#   first-run-experience:
#     prd: ...
# core-product:
#   ...
# growth:
#   ...
# enterprise:
#   ...
