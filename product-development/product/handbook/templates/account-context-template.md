---
updated: [YYYY-MM-DD]
areas: []       # optional — product areas this account is known to use
features: []    # optional — features this account is known to use
initiatives: [] # optional — initiatives this account is involved in (design partner, beta, escalation driver)
---

# [Account Name] ([account-slug]) — Account Context

## Who they are

- [2–4 bullets: what the company does, size band, vertical, how they use the product —
  segment fields mirror `accounts/portfolio.yaml`; this page carries the story, the YAML
  carries the queryable fields]

## Current truth

- [The live state of the relationship: what they're pushing for, open asks (link
  feature-request records), expansion signals, risks. Edit in place — newest truth
  replaces old; a change that matters becomes a decision record or a History line.]

## History

- [YYYY-MM-DD — event → [transcript](../../../user-insights/transcripts/YYYY-MM-DD-account-type.md) · [summary](calls/summaries/YYYY-MM-DD.md); newest first]

<!--
Template rules (delete this comment when filling):
- Copy to product-development/product/customers/accounts/{account-slug}/account-context.md.
- Living page: edit in place, bump updated:, budget ≤120 lines.
- The frontmatter link keys are OPTIONAL (link contract: governance/link-schema.yaml) —
  fill them as usage/involvement facts land (usually via /process-meeting or
  /context-update); slugs must resolve in the catalog / initiatives folder.
- Customer-side people appear as roles, not names (PII rule) — names live only in raw
  transcripts.
- Every processed call/interview adds one History line in the same change that files it.
-->
