# Prototype History

Pre-round snapshots, `{slug}-r{N}.html`, taken by `/prototype-feedback` before applying round N — so "what did it look like before?" stays answerable and rounds can be diffed.

Deliberately not listed file-by-file; the filename pattern is the index.
