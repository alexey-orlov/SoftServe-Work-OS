# Design System Cache

Cached Figma extraction shared by every prototype — `tokens.css` (the `:root` block pasted verbatim into each prototype) and `design-system.md` (where each token group came from, plus recorded gaps for the design-system owner).

Written by `/prototype` (extract or refresh — refresh only on explicit request); read by `/prototype-feedback`, `/prototype-challenge`, and the token audit (`.claude/skills/prototype/scripts/audit_tokens.py`).
